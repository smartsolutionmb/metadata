import { ITable } from "@/interfaces/ITable";
import React from "react";
const TableUpdate = ({
  tblId,
  setSidebarStatus,
  tblData,
}: {
  tblId: number;
  setSidebarStatus: any;
  tblData: ITable;
}) => {
  return <div>TableUpdate</div>;
};
export default TableUpdate;

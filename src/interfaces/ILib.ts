export type IFrequency = {
  id: number;
  code: string;
  name: string;
};

export type IUnit = {
  id: number;
  code: string;
  name: string;
};

export type ISecurity = {
  id: number;
  code: string;
  name: string;
};

export type IValueType = {
  id: number;
  code: string;
  name: string;
};

export type ISource = {
  id: number;
  code: string;
  name: string;
};

export type ICollectionMethod = {
  id: number;
  code: string;
  name: string;
};

export type IDissimenationLevel = {
  id: number;
  code: string;
  name: string;
};
